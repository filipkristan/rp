#ifndef PANEL_GET_WINDOW_IDS_HH
#define PANEL_GET_WINDOW_IDS_HH
#include <string>
#include <vector>

extern std::string command;
extern std::string command2;

std::vector<std::string> getWindowIDs();

#endif
